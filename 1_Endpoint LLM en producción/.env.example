# Plantilla de claves. Copia este fichero a .env y rellena los valores reales.
# El fichero .env está ignorado por git y NUNCA se sube al repo público.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
